# Smoke test 2026-09-13 — Flutter split build (branch flutter-split)

Build under test: `flutter-split` at a62b92fa4 (all `flutter/lib` files split to
300 lines, `tools/split/` added), Rust core unchanged apart from other sessions'
module splits. Purpose: prove that the part/extension based Dart splits run,
not only pass `flutter analyze`.

## Environment

| Role | Machine | Notes |
| --- | --- | --- |
| Build | 192.0.2.10 (Windows Server 2025, "mini") | Flutter 3.24.5 installed to `C:\build\toolchain\flutter`; `C:\build\build-flutter.ps1 <branch>` mirrors `build-local.ps1` (vcpkg static, `cargo build --release --features flutter,hwcodec,vram`, `dylib_virtual_display`, `flutter build windows`). `generated_bridge*.dart` are gitignored and must be copied from `C:\build\gen` like the `.rs` files. |
| Controller | this laptop, portable run of the built bundle (`openuu.exe` from the Release folder, stdout redirected) | An installed 1.5.0+68 OpenUU service was still running and owns `\\.\pipe\OpenUU\query`; the portable instance logs `ipc connection closed` every second but the UI works. |
| Peer 1 | 192.0.2.10, ID 100000001 | First as a portable run in the RDP session, then as a Windows service (`sc create … --import-config`, then `--service`), same LAN as the controller. |
| Peer 2 | Hyper-V VM vm 192.0.2.11, ID 100000002 | Service install of the same bundle, behind the Hyper-V NAT. |
| Servers | rs.example.com (hbbs 21116/21115, hbbr 21117, account 21114) | Same rendezvous key on all three clients and on the server (`id_ed25519.pub`). |

Build time on the build machine: Rust core about 7 min (warm cargo cache),
`dylib_virtual_display` about 1.5 min, `flutter build windows` 1 min 43 s.

## Steps and results

| Step | Result |
| --- | --- |
| Portable start, main window | OK — window renders, no Dart exception in stdout |
| Home: all devices / start assist / favourites (legacy page) | OK |
| Settings: general, security, network, display, about | OK (opened from the sidebar gear; the top tab bar does not react to synthesized clicks) |
| Account menu, logout entry, login dialog | OK — dialog renders; the "立即登录" button on the empty-state page did not open the dialog under synthesized clicks (unconfirmed, see below) |
| Remote window (multi-window part) | OK — opens for every `--connect`, dialogs render |
| Connect to a peer | FAILED — see defect 3; not caused by the Flutter split (same result with the pre-split build bfdc512c0 as controller and with old/new peer builds) |
| Picture / keyboard / mouse, RDP multi-session focus | Not run (blocked by the connection defect) |

`flutter analyze` on the build machine: 245 issues / 0 errors on every commit,
identical to the baseline before the split.

## Defects found

1. **Legacy signed-in session cannot connect.** Builds before the account gate
   stored the session only in the local `access_token`; the Rust gate
   (`src/account.rs`) reads the `openuu-account-token` option, so an upgraded
   install shows the user as signed in while every connection is refused with
   "Sign in to OpenUU before using remote connections". Fixed in 46d54296b
   (`UserModel._syncGateToken` copies the token to the gate key once the
   server confirms the session; 401/400 still resets both keys).
2. **`--import-config` does not carry `OpenUU2.toml`.** The installer flow
   (`sc create … --import-config <OpenUU.toml>`) copies only the ID/password
   file into the service profile; the custom rendezvous/relay/api/key options
   stay behind, so the service registers with `rs-ny.rustdesk.com` instead of
   the configured server. Worked around by copying `OpenUU2.toml` into
   `C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\OpenUU\config`.
   Assigned to session 8f (MSI fix).
3. **Signed-in controllers never reach the peer.** With an account token the
   client (`src/client/start_inner.rs`) secures the rendezvous TCP with a
   `KeyExchange` before sending `punch_hole_request`; the OpenUU hbbs has no
   `KeyExchange` handler, so the client waits 18 s and fails with
   "Failed to secure tcp: deadline has elapsed" without ever contacting the
   peer (confirmed with hbbs debug logs: two 18 s TCP sessions, no punch or
   relay records). Decision: implement `KeyExchange` in hbbs (session e9);
   client unchanged.

Observed but not verified: the empty-state "立即登录" button; peers in portable
mode inside an RDP session log `max ipc nack` from the portable service about
40 s after start (old and new builds alike) — retest after the service-mode
peers are back.

## Retest checklist (after the hbbs KeyExchange deployment)

1. Controller signed in as `test-account-1`, `openuu.exe --connect 100000002 --password …` (VM peer) and `--connect 100000001 --password …` (build machine peer): session established, hbbs logs punch/relay for both.
2. Picture visible and keyboard/mouse reach the peer (peer-side server log shows the input, a typed string appears in Notepad on the peer).
3. Build-machine peer with a second RDP session (`net user testrdp … /add`, log in from `mstsc`): the controller picture stays on the pinned session (f0583dd9b); delete `testrdp` afterwards.
4. Empty-state "立即登录" button opens the login dialog (manual click).
5. Both peers started from a fresh service install: `OpenUU2.toml` present in the LocalService profile without manual copying (defect 2 fix).
6. Local: stop the installed 1.5.0+68 service (UAC) before running the portable controller, confirm no `ipc connection closed` spam.

Artifacts: `~/openuu-smoke/` on the laptop (bundle, stdout logs, screenshots);
peer config scripts in this session's scratchpad (`peer-*.ps1`, `vm-peer-*.ps1`).
