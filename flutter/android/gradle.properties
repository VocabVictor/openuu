# 1024M could not finish a release build: R8 minify, the release signing
# validation and the lint pass each died with 'Java heap space' on a
# 16 GB CI runner. 4 GB gets through all three.
org.gradle.jvmargs=-Xmx4096M
android.useAndroidX=true
android.enableJetifier=true
org.gradle.daemon=false
